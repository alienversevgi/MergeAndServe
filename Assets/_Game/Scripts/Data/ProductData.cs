using UnityEngine;

namespace MergeAndServe.Data
{
    [CreateAssetMenu(fileName = nameof(ProductData), menuName = Const.SOPath.SO_DATA_MENU_PATH + nameof(ProductData), order = 1)]
    public class ProductData : ItemData
    {
    }
}